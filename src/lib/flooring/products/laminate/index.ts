/**
 * src/lib/flooring/products/laminate/index.ts
 *
 * Public API for the Laminate Flooring product family.
 *
 * To add a new Laminate collection: create `<slug>.ts` in this folder
 * exporting a typed `FlooringCollection`, then re-export it below.
 */

export { venice } from "./venice";
