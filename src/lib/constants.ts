/**
 * Global site constants.
 *
 * GOOGLE_REVIEW_URL — replace with the real Google "write a review" deep link
 * for the business (Google Business Profile → Share review form → copy link).
 * Kept centralized so it's never hardcoded inside a component.
 */
export const GOOGLE_REVIEW_URL =
  "https://g.page/r/REPLACE_WITH_WONDER_WALLZ_PLACE_ID/review";
